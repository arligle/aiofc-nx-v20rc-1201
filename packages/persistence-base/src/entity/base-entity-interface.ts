/**
 * 基础实体接口
 *
 * 该接口作为所有实体类的基础接口:
 * 1. 目前定义为空接口,为将来扩展预留空间
 * 2. 可以在此添加所有实体共有的字段和方法
 * 3. 通过继承该接口,可以确保实体类的一致性
 *
 * 使用场景:
 * - 作为实体类的基础接口
 * - 定义公共字段(如id、创建时间等)
 * - 统一实体类的结构
 */
export interface IBaseEntity {};
